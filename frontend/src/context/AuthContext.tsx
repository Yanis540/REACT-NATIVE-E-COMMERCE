import React from 'react';
import { Text, View } from 'react-native'

interface AuthContextProps {

};

function AuthContext({}:AuthContextProps) {
    return (
        <View>
           <Text>AuthContext</Text>
        </View>
    );
};

export default AuthContext;