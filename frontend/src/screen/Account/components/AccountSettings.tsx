import React from 'react';
import { Text, View } from 'react-native'

interface AccountSettingsProps {

};

function AccountSettings({}:AccountSettingsProps) {
    return (
        <View>
           <Text>AccountSettings</Text>
        </View>
    );
};

export default AccountSettings;