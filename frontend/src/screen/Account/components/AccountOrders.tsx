import React from 'react';
import { Text, View } from 'react-native'

interface AccountOrdersProps {

};

function AccountOrders({}:AccountOrdersProps) {
    return (
        <View>
           <Text>AccountOrders</Text>
        </View>
    );
};

export default AccountOrders;